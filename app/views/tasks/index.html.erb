<h1>Tasks manager</h1>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <input type="checkbox" disabled <%= 'checked' if task.completed %>> - <%= link_to task.title, task_path(task) %>
  </li>
  <% end %>
</ul>

<%= link_to 'Add new task', new_task_path %>
